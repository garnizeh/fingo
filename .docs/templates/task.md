# pN-NNN — <Task Title>

<!--
INSTRUCTIONS (delete this block before committing):
- Replace pN-NNN with the phase-prefixed sequence: p<phase>-<three-digit-number>
  e.g. p1-003 for the third task of Phase 1, p2-001 for the first task of Phase 2.
  The three-digit counter RESETS to 001 at the start of each new phase.
- Replace <Task Title> with a concise, action-oriented description.
- The filename of this file must also follow the pN-NNN-<slug>.md pattern
  (e.g. p2-001-create-credits-table.md). Do not change the filename after creation.
- A task file lives in either backlog/ (not started or in progress) or done/ (complete).
- When a task is completed, move the file from backlog/ to done/ — do not edit the
  filename. This preserves the history and makes completion visible at a glance.
-->

**Design Doc:** `.docs/design-docs/NNNN-<slug>.md`  
**Phase:** <phase number and name>  
**Status:** To Do | In Progress | Done  
**Assignee:** <name or unassigned>  
**Started:** YYYY-MM-DD  
**Completed:** —

---

## Goal

<!-- One sentence: what must be true when this task is done? -->

---

## Context

<!--
Why does this task exist?
What design doc section or phase does it belong to?
What decisions were already made that constrain the implementation?
One short paragraph is enough.
-->

---

## Acceptance Criteria

<!--
Concrete, binary checks. Each line must be verifiable by a reviewer.
Prefer "X exists and compiles" over "X is implemented".
-->

- [ ] <!-- e.g. `business/domain/creditcardbus/model.go` exists and exports `CreditCard`, `NewCreditCard`, `UpdateCreditCard` -->
- [ ] <!-- e.g. All fields in `QueryFilter` are pointer types -->
- [ ] <!-- e.g. `go build ./...` exits zero after this change -->
- [ ] <!-- e.g. Unit and Integration test passes -->

---

## Steps

<!--
Ordered list of concrete actions. Be specific about file paths.
Include relevant code snippets or SQL directly inline — do not link to external docs.
-->

1. <!-- e.g. Create `business/domain/creditcardbus/model.go` -->
2. <!-- e.g. Add `CreditCard` struct with `CreditCardID uuid.UUID` field -->
3. <!-- ... -->

---

## Notes

<!--
Optional. Decisions made during implementation, deviations from the design doc,
links to PRs or commits, follow-up tasks discovered.
-->
